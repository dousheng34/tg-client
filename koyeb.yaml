services:
  tg-client:
    build:
      context: .
      dockerfile: Dockerfile
    env:
      - name: TELEGRAM_BOT_TOKEN
        value: $TELEGRAM_BOT_TOKEN
    health_checks:
      disabled: true
    instance:
      type: nano
      regions:
        - fra
